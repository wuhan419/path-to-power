# 开发指南（DEVELOPMENT-GUIDE）

> 面向接手者。目标是：**新人 1 小时内能独立产出可上线的改动**，且**引擎团队和内容团队互不阻塞**。
> 配套文档：接口字段细节见 [`CONTENT-SCHEMA.md`](./CONTENT-SCHEMA.md)。

---

## 1. 一句话架构

```
        ┌─────────────────────────────────────────────┐
        │  内容团队（content/）  —— 高频改动、可无限扩   │
        │  事件 / 时代 / 出身 / 天赋 / 起点 / 轨道       │
        │  党派 / 派系 / 黑天鹅 / 填充模板 / 结局规则    │
        │  事件类型 / 量级 / 时代媒介时间轴               │
        └───────────────────┬─────────────────────────┘
                            │  POTUS.define(kind, payload)    ← 唯一接口
        ┌───────────────────▼─────────────────────────┐
        │  引擎团队（engine/）  —— 低频改动、契约稳定    │
        │  注册表 / 掷骰 / 效果 / 事件抽取 / 时间推进    │
        │  终局判定 / 界面渲染 / 程序化美术 / 存档       │
        └─────────────────────────────────────────────┘
```

**核心原则（不可违背）**

1. 引擎只提供**机制**，不含任何剧情与数值。
2. 内容只声明**数据**，不含逻辑分支。
3. 新增内容**永远不需要**修改 `engine/`。
4. 内容需要的新机制，由引擎团队实现为新字段并更新契约，**内容团队不得绕过契约 hack 引擎**。

**时间模型：一个月一回合。** 一个档期（slot）= 一次需要玩家决策的事件；
本月有没有档期、有几个、每个是什么量级，全部由「时代压力 + 玩家活跃度」决定。
内容作者通过 `era.pressure` 控制这个时代有多动荡，而不是去堆事件数量。
细读 [`CONTENT-SCHEMA.md` §0.5](./CONTENT-SCHEMA.md)。

---

## 2. 目录结构

```
game/
├── index.html                  加载器 + 内容包清单（加内容只改这里的 <script src>）
├── engine/                     ★ 引擎：稳定，改动需走契约审查
│   ├── style.css               界面样式
│   ├── core.js                 注册表 define() / 工具 / 状态 / 存档 / 平衡参数
│   ├── dice.js                 胜算公式 / 资源投注(stake) / 五档掷骰 / advantage
│   ├── effects.js              后果应用（含可扩展的效果键）
│   ├── events.js               事件筛选·抽取·填充·新闻
│   ├── time.js                 ★ 时间推进：年→月→档期、时代压力、量级抽取、时代脚本
│   ├── vignette.js             ★ 静好岁月：平静月的随笔拼装 + "按部就班"的成长结算
│   ├── llm.js                  ☆ 可选的大模型适配层（离线优先 / 超时降级 / 缓存），不接也完全能玩
│   ├── progression.js          终局判定（声明式规则）+ 结局页
│   ├── render.js               标题/建角/主循环(月历+年终)/状态面板/选项代价/投注面板/判定明细/静好岁月卡
│   └── art.js                  三级降级的程序化 SVG 插画（事件专属 → 类型默认 → 引擎兜底）
├── content/                    ☆ 内容：随便加，引擎自动适配
│   ├── 01-config.js            平衡参数 / 投注汇率 / 派系 / 轨道 / 党派 / 姿态 / 词条名 / 静好岁月成长曲线
│   ├── 05-categories.js        ★ 事件类型（每个类型自带默认配图与配色）+ 量级定义
│   ├── 06-media.js             ★ 时代媒介时间轴（报纸/广播/电视/互联网/短视频… 各自起始年）
│   ├── 07-contacts.js          ★ 人脉登记表（这一局里能反复出场的那些"人"）
│   ├── 08-vignettes.js         ★★ 静好岁月素材库：时令/世相/案头/日常/心绪/收束 六个槽位
│   ├── 09-photo-art.js         ★★ 事件配图的照片层（类型 → assets/events/<key>.jpg，缺图自动退回 SVG）
│   ├── 10-characters.js        出身 / 天赋 / 起点路径
│   ├── 20-eras.js              时代锚点 + 时代压力 + 定点事件 + 黑天鹅
│   ├── 30-fillers.js           填充模板（保证事件永不枯竭）
│   ├── 40-endings.js           结局规则（声明式）
│   └── events/
│       ├── 50-era-2008.js      按时代/主题分文件
│       ├── 51-era-1960.js
│       ├── 52-era-1974.js
│       ├── 55-media-timeline.js ★ 媒介时间轴：同一件事在广播/电视/短视频时代的不同形态
│       ├── 60-progression.js   跨时代晋升事件链（全部带 minTenure）
│       ├── 70-demo-resources.js  ★ 机制演示包（cost/stake 怎么写，抄它）
│       ├── 80-shady.js         灰产线样板：把柄 / 人脉 / 单次生效
│       ├── 82-press.js         媒体即武器样板：借笔→挡笔→用笔→拥有笔
│       ├── 84-archive.js       ★★ 事件链样板：四幕连演（拿到→被反噬→摊牌→了结）
│       └── 86-enclave.js       族群飞地样板：讲台 / 商户公所 / 双语选票 / 巡逻队
├── assets/
│   └── events/                 事件配图的照片（文件名 = 类型 key.jpg，见 §5.12）
├── tools/
│   ├── validate.js             ★ 一键校验（语法/引用/投注/模拟/契约），零依赖
│   ├── smoke-ui.js             ★ UI 冒烟（jsdom 真跑一遍点击流程），可选
│   └── preview-photos.html     ☆ 配图预览页（浏览器打开，看照片在事件卡里的实际观感）
├── docs/
│   ├── DEVELOPMENT-GUIDE.md    本文件
│   └── CONTENT-SCHEMA.md       接口契约
└── index.singlefile.bak.html   历史单文件版本（仅供对照，已不再维护）
```

**为什么是普通 `<script>` 而不是 ES module**：ES module 在 `file://` 下会被浏览器 CORS 拦截，导致"双击打不开"。用普通 script + 全局注册表，**双击 `index.html` 就能玩**，这也是内容团队最省事的分发方式。

---

## 3. 十分钟上手

```bash
# 1. 直接玩：双击 dev/index.html（无需任何构建、无需 npm install）
#    要分发给别人玩：bash dev/tools/package.sh 之后把 dist/ 整个文件夹发出去

# 2. 改完自检（数据 / 逻辑 / 300 局模拟 / 引擎-内容契约）
cd game && node tools/validate.js

# 3.（可选）UI 冒烟：真的用 DOM 跑一遍"建角→事件→投注→掷骰→结算"
#     需要 jsdom，只装一次（引擎本身零依赖）：
#     cd ~/.workbuddy/binaries/node/workspace && npm install jsdom
cd game && NODE_PATH=~/.workbuddy/binaries/node/workspace/node_modules node tools/smoke-ui.js
```

改一行看效果的最短路径：

1. 打开 `content/events/50-era-2008.js`
2. 找一个事件，把 `title` 改掉
3. 刷新浏览器（`index.html`），新开一局，选 2008 时代 —— 新标题就会出现
4. 跑 `node tools/validate.js`，确认"全部通过"

**不需要**重新构建、不需要重启服务、不需要改引擎。

### 第一步先看哪个文件？

| 你想写什么 | 先读哪个文件 |
|---|---|
| 机制（资源、投注、判定明细）长什么样 | `content/events/70-demo-resources.js` —— 官方演示包：代价含全部三种资源 / 缺钱变灰 / 精力稀缺 + 人情换重投。**写普通事件时抄它。** |
| **一件事跨越好几个月的那种故事** | `content/events/84-archive.js` —— 四幕事件链（拿到 → 被反噬 → 摊牌 → 了结），最后一幕按 `req.flag` 读出四种完全不同的收尾。**这是"事件太短"这个问题的标准解法。** |
| **一个反复出场的人** | `content/07-contacts.js`（登记表）+ `content/events/86-enclave.js` （用 `req.contact` / `effects.contact` 挂人） |
| **灰色路线 / 把柄怎么用** | `content/events/80-shady.js` —— 把柄的"得到"与"花掉"、单次生效（`unique`）、以及"把柄不能囤"的写法 |
| **让玩家别那么快爬到顶** | `content/events/60-progression.js` —— 每个晋升事件都带 `minTenure`（在位时长门槛） |

---

## 4. 分工与文件所有权

| 目录/文件 | 归属 | 说明 |
|---|---|---|
| `engine/**` | **引擎团队** | 内容团队只读 |
| `docs/CONTENT-SCHEMA.md` | **引擎团队**（双方评审） | 契约唯一来源 |
| `content/**` | **内容团队** | 引擎团队原则上不动 |
| `index.html` 的内容清单区 | **内容团队** | 加内容包时改这里 |
| `index.html` 的引擎引入区 | **引擎团队** | 加/删引擎模块时改这里 |
| `tools/validate.js` | **引擎团队**（双方可用） | 校验规则随契约演进 |
| `tools/smoke-ui.js` | **引擎团队**（双方可用） | jsdom UI 冒烟，验证机制真的在界面上跑通 |

### 双方职责

**引擎团队**
- 维护注册表、掷骰模型、效果系统、主循环、UI、存档
- 保证**向后兼容**：新增字段不能破坏已有内容
- 提供内容团队需要的机制（收到需求 → 设计字段 → 更新契约 → 实现 → 通告）
- 维护 `tools/validate.js` 的校验项，确保内容错误在提交前被拦住

**内容团队**
- 写事件、时代、人物线、结局、数值
- 只依赖 `docs/CONTENT-SCHEMA.md` 里写明的字段
- 提交前必须跑 `node tools/validate.js` 并通过
- 需要新机制时**提需求**，不自行改引擎

---

## 5. 内容团队：常见任务清单

### 5.1 加一个事件（最高频）

1. 选一个已有文件（如 `content/events/50-era-2008.js`）或新建 `<序号>-<分类>.js`
2. 追加到 `POTUS.define("event", [ ... ])` 数组里：

```js
{
  id: "2008_new_scandal",            // 全局唯一，建议加时代前缀
  era: ["2008_CRASH"],
  grade: "mid",                      // ← 必填：major / mid / minor（§4.8）
  category: "scandal",               // ← 必填：事件类型，决定默认配图（§4.8）
  medium: ["tv", "social"],          // ← 可选：这件事要靠哪种"说话方式"（§4.9）
  title: "标题",
  brief: {                           // ← 背景卡：主角此刻知道多少（§4.7）
    lede: "折叠时显示的一行",
    known:   ["他读到的数字、他认识的人、他手上的文件……"],
    rumor:   ["有人说…（真假不明）"],
    unknown: ["他不知道的事——点出后果的位置，不要点出后果"],
    terms:   [{ k: "救市", v: "名词解释" }]
  },
  body: "事件正文……",
  weight: 10,                        // 越大越容易被抽到（默认 10）
  tierMin: 0, tierMax: 5,            // 可选：只在某层级区间出现
  tracks: ["operative"],             // 可选：只在某些轨道出现
  // month: 9, day: 24,              // ← 只在"历史上真钉在某一天"时才写（§4.6）
  // fromYear: 2008, toYear: 2020,   // ← 绝对年份窗口（可选）
  choices: [
    { id: "a", text: "选项文案", base: 0.5,
      mods: [{ src: "attr", key: "CUN", w: 0.5 }],
      outcomes: {
        crit:     { body: "大成功文本", effects: { rep: 8, fac: { base: 10 } } },
        ok:       { body: "成功文本",   effects: { rep: 4 } },
        meh:      { body: "勉强文本",   effects: { rep: 1, fac: { base: -3 } } },
        fail:     { body: "失败文本",   effects: { rep: -2, fac: { base: -5 } } },
        critfail: { body: "大失败文本", effects: { rep: -6, flags: ["scandal_2"] } }
      }
    }
    // ……至少 2 个选项
  ]
}
```

3. 新建文件的话，在 `index.html` 的内容清单里加一行 `<script src="content/events/xx.js"></script>`
4. `node tools/validate.js`

**必须遵守**：五档结果 `crit/ok/meh/fail/critfail` 一个都不能少；每个选项的 `outcomes` 用**多行格式**写，不要压成一行手数括号（详见 §7）。

### 5.2 写背景卡：别把玩家丢进一个他看不懂的局面

这是踩过的最实在的一个坑。原来的"救市"事件全文是这样：

> **一份'救市'内部简报**
> 财政部的人深夜来电：救市方案需要'民间声音'背书。签名就能上电视，但条款里藏着一笔会让你未来的对手抓住的把柄。

测试者的第一反应是：**"什么是救市？为什么要救市？谁发动的？背后是什么情况？"** ——三个选项摆在面前，一个都判断不了。这不是玩家的问题，是内容的问题。

**修法是两块，缺一不可：**

**（1）给出精确日期。** 事件卡顶部显示 `2008 年 9 月 24 日`。日期不是装饰——**它是给"懂历史的玩家"的信息通道**。主角不知道四天后会发生什么，但玩家知道。

但注意：**`month` 是稀缺资源，只给真正钉在某一天的事件写。** 在"一个月一回合"模型下，写了 `month: 9` 的事件在 3 月是抽不到的；写多了会让事件池变浅。当前只有 7 个事件带日期，都是史实锚点：

| 事件 | 日期 | 为什么是这个日期 |
|---|---|---|
| 一份「救市」内部简报 | 2008-09-24 | 雷曼已倒、AIG 已被接管，TARP 正在国会拉锯 |
| 市镇厅会议上的怒吼 | 2009-04-15 | 茶党运动在全国市镇厅同日发难 |
| 初选前夜 | 2012-04-30 | 大选年的初选季 |
| 第一次电视辩论 | 1960-09-26 | 肯尼迪–尼克松首场电视辩论的当天 |
| 南方的一座教堂 | 1963-10 | 民权运动白热化的秋天 |
| 一份掉在你桌上的文件 | 1974-05-09 | 水门余波里的泄密 |
| 听证会的传票 | 1974-07-24 | 最高法院裁决总统必须交出录音带的那一天 |

**想给一个虚构事件也钉日期？** 用 `era.scheduled`（见 CONTENT-SCHEMA §1.1），把「哪一年哪一月必定发生」写在时代里，而不是写在事件里——这样事件本身还保持着"任何月份都可能"的通用性。时代脚本已经这样用在了三场时代大事件上。

**（2）给出主角视角的背景卡。** 三层，折叠可展开，默认展开、折叠状态被记住：

- **你确知的** —— 主角**真的能看到**的东西：他手上的文件、他认识的人、报纸上印着的数字。**不是上帝视角的历史总结。**
- **你听到的 · 真假不明** —— 传闻。用「有人说…」「有律师朋友在饭局上压低声音说…」。其中可以有真话，也可以是别人故意放出来的假消息，**不要用语气暗示真假**。
- **你尚不知道的** —— **这一层是灵魂**。它明确告诉玩家"这里有信息差，你可以占便宜"。写法是点出**后果的位置**，不是点出后果本身：
  - ✅ `你读不懂的那几条授权条款，将来会成为对手手里的刀。`
  - ❌ `两个月后国会会否决这个法案，你会因此丢掉席位。`（这是剧透，不是信息差）
- **名词**（可选）—— 只解释名词（救市 / TARP / 初选 / 弹劾条款），**不解释剧情**。让不熟悉这段历史的玩家也能跟上。

写完自检一句话：**这份背景卡里，有没有主角此刻不可能知道的事？有就删掉或挪进 `unknown`。**

当前 61 个事件 **全部**配齐了背景卡、量级、类型。写新事件时直接抄 `content/events/50-era-2008.js` 里的 `2008_crash_offer`。

### 5.3 用资源做设计：代价（cost）与投注（stake）

这是本作最核心的"手感"机制——**资源不是装饰，是玩家下注的筹码**。完整字段说明见 `CONTENT-SCHEMA.md` §4.3 / §4.4，这里只讲"什么时候用哪个"。

**三个字段的分工：**

| 想要的效果 | 用哪个 | 玩家看到什么 |
|---|---|---|
| 没资格，压根选不了 | `req` | 按钮灰掉 +「需要 T3 以上」/「需要先认识「老雷」」 |
| 能选，但要先付钱/精力/把柄 | `cost` | 按钮上「代价：资金 $400k」/「代价：把柄 1」 |
| 可押可不押，玩家自己赌 | `stake` | 点开后弹投注面板，自己拖金额 |
| 给「人情」这个资源找独特价值 | `stake: { fav: true }` | 「花 1 点，获得重投（取优）」 |

**五种资源的性格（写事件时的直觉）**：

| 资源 | 性格 | 什么时候用它当代价 |
|---|---|---|
| `fun` 资金 | 可再生的、最不值钱的 | 每天都在流失，随便花；它是"耐心"的度量 |
| `ap` 精力 | 每年才恢复一次，最稀缺 | 表达"这一件事要占掉我一段时间" |
| `fav` 人情 | 借来的，要还 | 表达"我要求人了"；稀缺，别乱用 |
| `lev` **把柄** | **易腐、会招祸、不可再生** | 表达"我要用一次别人不敢说的东西"——**用它的时候，剧情应该往前走一大步** |
| `rep` / `hp` | 一旦掉下去很难回来 | 只在"这件事真的伤到你的名声/身体"时才用 |

**一个写得好的事件通常长这样**（直接抄 `content/events/70-demo-resources.js`）：

```js
choices: [
  { id: "all_in",  text: "亲自出庭，把能押的都押上", base: 0.42,
    cost: { ap: 1 }, stake: { fun: true, ap: true, fav: true } },   // 便宜但风险高 → 让玩家自己下注
  { id: "lawyers", text: "重金请顶级律师团（花 $400k）", base: 0.72,
    cost: { fun: 400000 } },                                        // 花钱买确定性
  { id: "favor",   text: "托人推迟议程（花 1 点人情）", base: 0.62,
    cost: { fav: 1 } },                                             // 钱买不到的路子
  { id: "bigshot", text: "动用参议院的老关系", base: 0.82,
    req: { tier: 3 } },                                             // 资历门槛
  { id: "sick",    text: "病假缺席，什么都不花", base: 0.45,
    stake: { ap: true } }                                           // ← 保底选项：无 cost、无 req
]
```

五种选项凑在一个事件里（免费高风险 / 花钱买稳 / 消耗人情 / 资历门槛 / 保底），玩家的每一次点击都是一次真实取舍。

> **最后那条 `sick` 不能省。** 前四种都带 `cost` 或 `req`，玩家破产时会被全部置灰——没有它，这个事件就是死局。
> 保底选项要写得像个真实选择（"病假缺席"本身就有风险：可能被拍到在高尔夫球场），而不是敷衍的"什么都不做"。

**数值设计的四条经验（别踩坑）：**

1. **代价要肉疼但不致命**。重代价选项一个事件最多 1~2 个；`fun` 允许负值（负债有戏剧性），但别把 `rep`/`hp` 当常规代价用——容易造成死亡螺旋。
2. **花了钱就必须更稳**。参考锚点：免费 `base ≈ 0.40~0.50`，花 `$400k` 应给到 `0.70+`，消耗 1 点人情给 `0.60~0.65`。
3. **`stake` 只给"赌注该由玩家决定"的选项**。固定代价的选项别加 `stake`，否则玩家要操作两遍同样的东西。
4. **投注上限是 `+30%`（钱）`+9%`（精力），且总目标值封顶 95%**——所以别指望"投注"能救回一个 `base 0.2` 的选项。低 `base` 就该配高风险高回报的五档结果。
   另外记住精力只有 **3 档**（`+3%/点`，上限 `+9%`）、资金 **8 档**（`$250k/档`，上限 `+30%`）——上限之外的档位引擎直接不给投，界面上按钮会置灰。

**想改汇率？** 改 `content/01-config.js` 的 `balance.stakeRates`，全局生效；单个选项要特例就在 `stake` 里写 `{ per, w, cap }` 覆盖。

### 5.4 加一个时代

1. `content/20-eras.js` 里加一条 `era`（`name / startYear / brief / pressure`）
   - **`pressure` 是这个时代最重要的一个数字**：它决定这一年有多忙、大事件多不多。
     危机年代写 `{ "2008": 5, "2009": 5, "*": 2 }`；长期平稳的年代就给个 `2`。
2. 新建 `content/events/5x-era-<id>.js` 写该时代事件（`era: ["<id>"]`）
3. 把该时代的关键节点写进 `era.scheduled`（哪年哪月必定发生什么）
4. `content/20-eras.js` 的 `blackswan` 里加该时代的黑天鹅
5. `content/30-fillers.js` 里加该时代的填充包（**强烈建议**，否则会退化用 `"*"` 兜底包）
6. `index.html` 清单里引入新事件文件
7. `node tools/validate.js`

一个时代的**最小可玩内容量**建议：≥ 25 个事件 + 3 个黑天鹅 + 1 个填充包。

> **为什么是 25 而不是 10**：一个月一回合之后，一年要消耗 4~8 个档期。
> 事件池太浅会同时导致三件事：填充事件占比上升、日期自洽率下降、同一个事件反复出现。
> `validate.js` 会把这三个数字都打出来。

### 5.4.1 加一个事件类型（category）

只改 `content/05-categories.js`：

```js
POTUS.define("category", {
  "labor": {
    name: "劳工", ink: "#4a5a2a", base: "#e6e9d6",
    art: function (ev, P, ctx) {
      return P.artFrame('<rect .../>' + ctx.caption(ev, ctx.ink), ctx.ink, ctx.base);
    }
  }
});
```

之后任何事件写 `category: "labor"` 就能用上这套配色和默认配图。
**每个类型只需要画一张图，之后所有该类型的事件都免费有图**；个别事件想特殊，自己写 `art` 覆盖即可。

### 5.4.2 加一个时代媒介（medium）

只改 `content/06-media.js`：

```js
POTUS.define("medium", {
  "podcast": { name: "播客", from: 2014, note: "长谈重新变得有市场……" }
});
```

之后事件写 `medium: "podcast"`，引擎就会自动挡在 2014 年之前。
**这是"让内容按年代生效"最省事的办法**——不用写任何逻辑，也不用在事件里堆 `fromYear`。

### 5.4.3 把内容派给不同的人写（分工）

因为每个事件都必须声明 `grade` + `category`，内容可以按 `时代 × 类型` 切成片派活：

| 方式 | 适合 | 交付物 |
|---|---|---|
| 按「时代 × 类型」切片 | 人多、要并行 | `content/events/5x-<时代>-<类型>.js`，只放这一类 |
| 按「时代」切片 | 一个人吃下一个时代 | `content/events/5x-era-<id>.js` |
| 按「类型」切片 | 一个人专精一条线，跨时代写 | `content/events/5x-<类型>.js`（范例：`55-media-timeline.js`） |

**交付格式就是 §5.1 那一块，复制填完即可。** 提交前跑 `node tools/validate.js`，
脚本会分别报出**量级分布 / 类型分布 / 时代专属占比 / 日期自洽率 / 填充事件占比**——
谁写的那一片内容太薄，这几个数字会直接指出来。

### 5.5 加派系 / 轨道 / 党派 / 姿态 / 出身 / 天赋 / 起点

全部只改 `content/01-config.js` 与 `content/10-characters.js`，**界面会自动多出选项**，不需要动引擎。

```js
// 加一个派系
POTUS.define("faction", { tech: { name: "科技巨头" } });
// 之后任何事件的 effects.fac.tech 和 mods {src:"fac",key:"tech"} 都能用
```

```js
// 加一个起点路径
POTUS.define("entry", {
  unionist: { name:"工会干事", desc:"…", tier:0, track_suggest:"electoral",
              effects: { fac:{labor:45, commercial:-20} } }
});
```

> ⚠️ 注意：加高影响力的派系/身份后，要检查既有事件的 `effects.fac` 是否还平衡。

### 5.6 加一个结局

只改 `content/40-endings.js`，加一条带 `priority` 的规则：

```js
{
  id: "retire_union", priority: 86,
  when: { reason: "retire", entryIn: ["unionist"] },
  title: "工会的传奇", grade: "B",
  body: "你从未离开过那条街。"
}
```

规则按 `priority` 降序取**第一条命中**；确保最后永远有一条 `when: {}` 的兜底规则（校验脚本会检查）。

### 5.7 写一条事件链：让一件事横跨好几个月

**这是"事件和流程太短"的标准解法。** 普通事件是"一个月里的一件事"，事件链是"一件事在几个月里长成另一个样子"。

最小可用的四幕结构（抄 `content/events/84-archive.js`）：

```js
// 第一幕（头）：正常写，必须 unique
{ id: "archive_get", grade: "mid", unique: true, … }

// 第二幕：前情 + 岔路
{ id: "archive_bite", grade: "mid", unique: true,
  flags: ["archive_taken"],                                    // 只在"把东西带走了"这条岔路上继续
  after: { id: "archive_get", minMonthsAfter: 3, maxMonthsAfter: 30 }, … }

// 第三幕：摊牌
{ id: "archive_showdown", grade: "major", unique: true,
  after: { id: "archive_bite", minMonthsAfter: 2, maxMonthsAfter: 24 }, … }

// 第四幕（收尾）：按 req.flag 分支，让结局回到前面的选择上
{ id: "archive_settle", grade: "mid", unique: true,
  after: { id: "archive_showdown", minMonthsAfter: 4, maxMonthsAfter: 48 },
  choices: [
    { id: "after_deal",   req: { flag: "archive_deal" },   … },   // 当年用它换了位置，现在账单来了
    { id: "after_burn",   req: { flag: "archive_burn" },   … },   // 当年烧了纸，但原件还在别人手里
    { id: "quiet",        … }                                     // ← 保底：无 cost、无 req
  ] }
```

**四条硬规矩**（`CONTENT-SCHEMA.md` §4.11 有完整表格）：

1. 链上**每一幕都要 `unique: true`**。`grade: "major"` 默认就是 true，其他量级必须显式写。
2. **用 `after` 表前情，用 `flags` 表岔路**。不要拿 flag 当"前情"——玩家没拿到那个 flag 时链会悄悄断掉。
3. **最后一幕用 `req.flag` 分支**，而且**每个选择、每一档结果都要打上同一个标记**。否则分支读不出来。
4. 分支之外**永远留一条既无 `cost` 又无 `req` 的路**。

写完之后 `validate.js` 会检查：前情是否存在、有没有自环或环、窗口 `min ≤ max`、以及**实测续集加权是否接近 `chainWeightMul/(chainWeightMul+1)`**（默认 ≈ 90%）。
这个 90% 是关键——它保证"玩家真的会看见第二幕"，而不是让故事散落在几百个档期里。

> **窗口怎么定**：`minMonthsAfter` 决定"最早多久以后"，`maxMonthsAfter` 决定"过期时间"。
> 一般 3~30 个月（半年级）最像真实政治；跨度拉太长（>48 个月），玩家会忘了前情。
> 界面上会给你一条「承 前」小条，写着上一幕的标题和相隔几个月——所以哪怕玩家真的忘了，他也能接上。

### 5.8 登记一个人脉：让同一个人反复出场

一个事件里写「你去找他」很容易，难的是让同一个"他"在十年后再次出现，并且记得上次你欠了他什么。

**两步**：

```js
// ① content/07-contacts.js —— 先登记（只写设定，不写年代）
POTUS.define("contact", {
  fixer: { name: "老雷", role: "掮客", tag: "门路", note: "他不解决问题，他介绍解决问题的人。" }
});

// ② 事件里引用 —— 门槛 / 好感 / 断交
{ id: "ask", text: "让老雷替你牵一根线", req: { contact: "fixer" }, … }   // 不认识就点不动
{ id: "deep", contacts: ["fixer", "lobbyist"], … }                        // 事件级：两个都要认识
effects: { contact: { fixer: 12 } }        // +12（第一次出现即"从此认识"）
effects: { forget: ["fixer"] }             // 彻底断掉（你把他卖了）
```

**三条约定**：
1. **一个人只负责一类资源**。找钱的别来找他要选票，要选票的别来找他借钱。
2. **人物不写死年代**——1960 年的电视制作人与 2008 年的电视制作人是同一个人设。要限定年代就写在**事件**上。
3. **文案里的名字要和 id 对得上**。校验器会把 `ev.contacts` / `req.contact` / `effects.contact` / `effects.forget` 里的每个 id 逐条核对；id 不一致是内容团队最常自己踩的坑。

### 5.9 让晋升变慢：`minTenure`

光有事件不够，**台阶本身也要花时间**。给晋升事件加一个"在位时长"门槛：

```js
{ id: "prog_senate", grade: "major", minTenure: 48, … }   // 在 T3 待满 4 年，才谈得上参议院
```

- 单位是**月**。层级一变，计时自动重置（引擎侧），状态面板会显示「在位 X 个月」。
- 建议值：基层 / 州级 18~24；参议院 / 总统 48；任命 / 造王者 / 巨富 18；名人 12。
- **所有 `prog_*` 晋升事件都必须声明 `minTenure`**——校验器会拦。
  确切的数字会直接决定"一局有多长"，所以这是**平衡参数，不是内容参数**：改它之前先想清楚一局想玩多久。

### 5.10 写一段"静好岁月"：让平静的月份也有人味

**为什么需要它**：一个月一回合之后，一年有 **8 个月左右是平静的**（`validate.js` 实测 8.1 个档期/年）。
这些月份原本只在月历条上留一行「平静」二字——可一个人的一生，大部分时间就是在这样的月份里过去的。
`content/08-vignettes.js` 就是给这些月份写的内容：**一段日子 + 一点按部就班的成长**。

**怎么做**：往 `content/08-vignettes.js` 里加片段即可（新文件也行，`fragments` 会累加）。

```js
POTUS.define("vignette", {
  fragments: [
    { id: "se03", slot: "season", months: [3], texts: ["三月。雪化得比往年早。", "三月，树还是光的。"] },
    { id: "wk_el_lo", slot: "work", tracks: ["electoral"], maxTier: 2,
      texts: ["晚上又是一场自助餐。你握到第八十只手的时候，已经分不清谁是谁了。"] },
    { id: "lf_gen", slot: "life", brk: true, texts: ["日子过得很快，也很整齐。"] },
    { id: "sf_lev", slot: "self", minLev: 1, weight: 3,
      texts: ["夜里你会想起那件事。办公室里没有别人，你把那份东西拿出来看了一眼，又放回去。"] }
  ]
});
```

引擎把六个槽位按固定顺序拼成一段：**时令 → 世相 → 案头 →〔断段〕日常 → 心绪 →〔断段〕收束**。
每个槽位抽一条可用片段（条件不满足就跳过），所以同一段文字会随主角**变老、升官、生病、心事变重**而变形。

**四条硬规矩**（校验器会拦前三条）：
1. **时令必须覆盖 12 个月**；
2. **除时令外，每个槽位至少留一条"无条件片段"**——玩家可能同时病重、破产、被调查、年迈，条件写满了反而一段都抽不出来；
3. `slot` 只能是上面六个之一，`id` 唯一，`weight` 为正；
4. **不要有转折**。它不是事件，是日子。写成小高潮，就把事件的活儿干了两遍，而且显得平静的月份一点也不平静。

**成长曲线在 `balance.vignette` 里，不在片段里**：详见 `CONTENT-SCHEMA.md` §4.13。
一句话提醒：`hpChance` 默认 **0**，是刻意的——这个游戏的死亡率正好卡在退休年龄那条线上，
平静月只要回一点点健康，就会把一大批"累死"翻成"退休"（实测 `0.04` 就让因病去世从 ~120 掉到 48）。
想给玩家"喘口气"的手感就去调它，但调完**一定要跑 `validate.js` 看结局分布**。

### 5.11 接一个大模型来润色（可选，不接也完全能玩）

引擎内置一层可选适配（`engine/llm.js`）。它只做两件事：给「静好岁月」**润色**，或者**另写一段**。
铁律是 **离线优先**：引擎先把模板拼好渲染出来，模型回来了才替换；模型不回、报错、被墙，玩家看到的仍是一段完整的话。

**玩家侧怎么用**：游戏里工具条上的 `✨ AI 未接` → 填接口地址 / 模型 / 密钥 → 保存。
密钥只存在浏览器的 `localStorage`（键 `potus_llm`），不进代码、不上传别处。
「静好岁月」卡上会因此多出「✨ 让 AI 润色」「🎲 另写一段」两个按钮；设置里还能开「自动润色」。

**开发者侧要改什么**：
- 改 prompt：`engine/llm.js` 的 `buildPrompt(kind, ctx)`（`vignette_polish` / `vignette_fresh`）。
  `stateBrief()` 负责把局面写成人话，其中**会自动列出"这个年代还不存在的媒介"**——这是防止模型在 1960 年写出智能手机的那道闸。
- 换厂商：接口按 OpenAI 的 `/chat/completions` 形状发，返回用 `extractText()` 兼容了
  OpenAI chat / Responses / Anthropic / Gemini 四种形状；特殊网关可以用设置里的「自定义请求头」。
- **file:// 的跨域**：浏览器 `fetch` 会带 `Origin`，多数厂商不给 `file://` 放行。这不是 bug。
  用 `python3 -m http.server` 打开，或者填一个自己搭的转发地址。

**成本护栏**：`balance.aiCallCap`（默认 80 次/局）+ 结果按 prompt 哈希缓存（上限 60 条，同样的局面不重复花钱）。
`validate.js` 会检查"没配置时是否安全降级""prompt 里是否混进了密钥""缓存是否超容""额度是否真的封顶"。

### 5.12 给事件配一张照片

事件配图默认是**程序化 SVG**（`content/05-categories.js` 每个类型一套）。想换成真照片，
走**照片层**：`content/09-photo-art.js` 把「类型 → 文件名」登记进注册表，配图时优先出照片。

**三步**：

1. 把图丢进 `assets/events/`，命名成 **`<类型 key>.jpg`**（例如 `career.jpg`、`scandal.jpg`）。
   想换图就直接覆盖同名文件，**不用改任何代码**。
2. 在 `content/09-photo-art.js` 的 `files` 里加一行：`career: "career.jpg"`。
3. 跑 `node tools/validate.js` —— 它会**逐个检查这些文件在不在于磁盘上**（这是最容易静默翻车的地方）。

**两条降级保证**（所以照片是"可加可减"的，不会因为少拷文件而白屏）：

- 没登记照片的类型 → 安静退回程序化 SVG。
- 照片加载失败（改名 / 少文件）→ 卡片里预垫的程序化 SVG 会被 `onerror` 翻出来顶上。

**单事件覆盖 / 关闭**：`ev.photo = "my-shot.jpg"` 用指定图；`ev.photo = false` 不要照片。

**尺寸建议**：卡片正文列宽约 636px，图压到**最大边 900px / JPEG q78** 就够（十张约 860 KB）。
画框是 `aspect-ratio:16/9` + `object-fit:cover`，所以原图比例不必统一，但**主体尽量居中**，否则会被裁掉。

**想看效果**：浏览器打开 `tools/preview-photos.html` —— 它用真的渲染器 + 真的样式表铺出事件卡，
把 12 个类型和"破图态"一次看全，不用真的开局。

---

## 6. 引擎团队：改动的纪律

### 6.1 可以改
- 新增机制字段（如"事件链""多阶段竞选""NPC 关系"）——**先改契约，再改实现**
- 修复 bug、优化性能、改进界面
- 调整 `tools/validate.js` 的校验覆盖

### 6.2 不可破坏的契约（破坏 = 所有内容失效）
1. `POTUS.define(kind, payload)` 的 kind 与 payload 形状
2. 事件的字段名与语义：`id/era/title/body/weight/tierMin/tierMax/tracks/parties/flags/notFlags/minRep/minFun/cond/choices/month/day/brief`
3. 选项：`id/text/base/mods/req/cost/stake/outcomes`，且 `outcomes` 五档键名固定为 `crit/ok/meh/fail/critfail`
4. `effects` 的键名与取值范围
5. `mods` 的 `src` 枚举与计算公式
6. 结局规则的 `when` 字段与 `priority` 语义
7. 存档结构 `POTUS.G` 的字段名（存档兼容性依赖它）
8. 资源键名固定为 `fun / fav / ap / rep / hp`，投注键名固定为 `fun / ap / fav`（`POTUS.stakeSpec/stakeInfo/computeP` 的入参契约）
9. `brief` 的分段键名固定为 `lede / known / rumor / unknown / terms`；`terms` 元素形状固定为 `{k, v}`
10. `month` 取值 1-12、`day` 取值 1-31；引擎的日期口径 `POTUS.dateText(ev)` 与报头/状态面板同步

### 6.3 允许的兼容性变更方式
| 想做的事 | 正确做法 |
|---|---|
| 给事件加新字段 | 新增**可选**字段 + 引擎提供默认值 |
| 改某个字段语义 | 新增字段，旧字段保留为别名，标注 deprecated |
| 改效果取值范围 | 不要改；新增独立效果键 |
| 重命名字段 | 引擎同时接受新旧名（过渡期），契约里写迁移说明，下个大版本移除 |
| 必须破坏兼容 | 升 `POTUS.VERSION` 主版本号 + 在 `docs/CHANGELOG.md` 写迁移指南 + 通知内容团队 |

### 6.4 引擎改动后的自检
```bash
node tools/validate.js     # 必须"全部通过"
# 动过 render.js / dice.js / style.css 的话，再跑一次真 DOM 冒烟：
NODE_PATH=~/.workbuddy/binaries/node/workspace/node_modules node tools/smoke-ui.js
```
校验脚本会自动读取 `index.html` 的清单，因此内容团队新加的文件也会被一并校验。

---

## 7. 内容写作规范（血泪教训）

### 7.1 多行格式，禁止单行压缩
这是本项目**踩过最大的坑**：早期把整个选项压成一行手写括号，产生了大量隐蔽的括号失衡，`node --check` 报错点还会漂移，排查成本极高。

**正确写法**
```js
choices: [
  { id:"sign", text:"签名背书", base:0.5, mods:[{src:"attr",key:"CHA",w:0.5}],
    outcomes:{
      crit:{ body:"…", effects:{ rep:12, fac:{ establishment:15 } } },
      ok:{ body:"…", effects:{ rep:6 } },
      meh:{ body:"…", effects:{ rep:3, flags:["stain"] } },
      fail:{ body:"…", effects:{ rep:-2 } },
      critfail:{ body:"…", effects:{ rep:-8, fac:{ press:-15 } } }
    }
  },
  { id:"refuse", /* 第二个选项 */ }
]
```

**判断标准**：每个 `outcomes` 块的闭合括号应当**一眼可见**，不需要数。

### 7.2 其他约定
- 字符串统一用双引号；正文里的中文引号用 `'` `'`，不用转义
- `id` 全局唯一，加前缀（`2008_` / `prog_` / `affair_`）
- 事件里**不写逻辑**；需要条件用 `cond(G)`，需要门槛用 `req`
- 事件里不引用不存在的 `track/party/faction/era` 键（校验脚本会报错）

### 7.3 数值设计参考
| 项 | 建议 |
|---|---|
| `base` | 0.35（逆风）～ 0.7（顺风）；纯运气事件用 0.5 |
| `weight` | 主线 14-20，支线 8-12，氛围 5-8 |
| 大成功收益 | ≈ 成功收益 × 2，并可附加 `flags` 解锁后续支线 |
| 失败代价 | 属性小幅下降 + 派系好感 -4~-10；**不要直接扣健康** |
| 大失败 | 引入不可逆状态（`scandal_2+` / `investigation_open` / `prison`） |
| 丑闻分级 | 1=流言，3=立案，5=终局（会触发入狱结局） |
| 层级跃迁 | 用 `prog_*` 系列事件的 `effects.tier:1`；失败**不给**层级 |

**平衡红线**：`balance.recentCap` 必须**小于**事件总数，否则事件池会被去重窗口掏空、退化为填充事件，层级永远升不上去（校验脚本会拦这一条）。

**月度回合带来的一个副作用（务必知道）**：一个月一回合之后，**每局的事件总量比原来"一年几回合"多得多**，
于是层级提升、资源累积都会变快。已经做的三处补偿是：

1. `prog_*` 晋升链全部标记 `unique: true` —— 每个仕途里程碑一局只演一次，晋升速度不再随事件数增长；
2. 机制演示包权重从 30/28/26 降到 6/5/4 —— 演示内容不再淹没正式内容；
3. 新增 `balance.eraWeightMul`（默认 3）—— 时代专属事件的权重乘以 3，避免被跨时代通用事件压过去。

调平衡时**先跑 `validate.js` 看层级分布与结局分布**，再看量级/类型分布。如果 300 局里有超过一半打到 T5，
说明晋升还是太快，优先调 `prog_*` 的 `weight` 或 `tierMin/tierMax`，而**不是**去砍 `activeChance`（那会让游戏变得无聊）。

---

## 8. 协作流程

### 8.1 内容团队提交清单
- [ ] `node tools/validate.js` 全部通过
- [ ] 新增事件：五档结果齐全、`id` 唯一、`era` 存在
- [ ] 新增事件：**`grade` + `category` 都写了**（界面徽章与配图依赖它们）
- [ ] 新增事件：**`brief` 写了**（玩家不该被丢进看不懂的局面）
- [ ] 写了 `month` 的话，确认它是**史实锚点**（不是"随便挑个季节"）
- [ ] 依赖某种媒介的事件，写了 `medium`（而不是硬编码 `fromYear`）
- [ ] 新增文件：已在 `index.html` 内容清单登记
- [ ] 新增时代：era（含 `pressure`）+ 事件 + 黑天鹅 + 填充包 四件套齐备
- [ ] 数值不越界（`base` 在 0-1、行列在 schema 范围内）
- [ ] 动到 `lev` / `cost` / `stake` 的话：该事件仍有**保底选项**（无 `cost`、无 `req`）
- [ ] 引用了人脉（`contacts` / `req.contact` / `effects.contact` / `effects.forget`）→ 对应 id **已在 `reg.contact` 登记**（`07-contacts.js`）
- [ ] 写了事件链（`after`）→ 每一幕 `unique: true`，且晚一幕**自己声明 `flags`**，不把"前情"当必然
- [ ] 写了晋升事件（`prog_*`）→ 带 `minTenure`（否则会在同层级被反复刷出）
- [ ] 写了「静好岁月」片段（`content/08-vignettes.js`）→ 时令覆盖 12 个月、每个槽位留一条无条件片段、**没有转折**（见 §5.10）
- [ ] 加了照片（`content/09-photo-art.js`）→ 文件名**确实在 `assets/events/` 里**、类型 key 在 `reg.category` 里存在、图压过（≈900px/q78）（见 §5.12）
- [ ] 看一眼 `validate.js` 的**填充占比 / 日期自洽率 / 时代专属占比 / 类型分布**四个数字是否健康

### 8.2 引擎团队提交清单
- [ ] `node tools/validate.js` 全部通过
- [ ] 契约有变化 → 已更新 `docs/CONTENT-SCHEMA.md`
- [ ] 破坏性变更 → 已升 `POTUS.VERSION` 主版本 + 写迁移说明
- [ ] 已有内容包**无需修改**即可继续运行（向后兼容自测）

### 8.3 冲突处理
- 内容团队需要新机制 → 提需求（描述场景 + 期望字段形状）→ 引擎团队设计并更新契约 → 双方确认后引擎实现
- 不要"先在事件里写个 hack、回头再改" —— 这类临时逻辑会永久留在内容里，成为接手者的地狱

---

## 9. 自动化校验

### 9.1 `tools/validate.js`（零依赖，必跑）

一条命令覆盖下面全部类别：

| 阶段 | 检查内容 |
|---|---|
| 语法 | 逐文件解析（等价 `node --check`） |
| 加载 | 按 `index.html` 清单顺序执行，捕获启动崩溃 |
| 引用完整性 | 事件/填充/黑天鹅引用的 era、track、party、faction、entry 是否存在；event id 是否重复；**每个事件是否有 `grade` / `category`（且值在注册表里）**；`medium` 的键是否存在；`fromYear/toYear/years` 是否自洽；五档结果是否齐全；`base` 是否在 0-1；`cost` 的键与数值合法性；`stake` 的键与形状且 `cap ÷ w ≥ 1`；`month/day` 取值范围；`brief` 的四段结构与 `terms` 形状；媒介时间轴自身的 `from`/`note` 是否齐全；结局是否有兜底规则；`recentCap < 事件总数`；**每个事件都有一个「无 `cost` 且无 `req`」的保底选项**（防止资源见底时所有按钮都点不动） |
| 背景卡 / 时间 | 统计 `brief` 与 `month` 的覆盖率；逐时代确认至少有一个事件带背景卡；校验 `dateText` 的年月日格式化与月份不可回退 |
| **月度回合 / 量级 / 媒介** | 时代压力随年份变化、丑闻会把活跃度顶上去；压力 → 大/中/小事的分布单调性（压力高→大事件多）；时代脚本在指定年月必定命中且一局只演一次；低压力年份能抽到「平静的月份」；走一遍完整月历统计档期数；日期自洽率；**媒介时间轴门控**（1700 年不放行电视、1960 年不放行短视频、2025 年放行合成影像）；`years` 窗口；`mediaNow()` 的年度媒介清单 |
| 掷骰分布 | 1 万次投掷，验证 crit≈15%、critfail≈3% |
| 资源经济 | 投注加值/花费计算、余额夹取、95% 封顶、advantage 分布优于单次 |
| **投注档位上限 / 死局保护** | `P.stakeMax` 的两道闸（`ceil(cap÷w)` 与余额）：精力 +3%/点、上限 +9% → 无论有几点精力都只能投 3 点；资金吃满 +30% 需要 8 档；一档都投不起时上限为 0；加成单调不减且永不越上限；`P.fallbackIndex` 在全堵死时放行恰好一个、有可选项时不干预 |
| 模拟对局 | 300 局全自动跑通（**走真实的 `advanceMonth` 月度推进**，每局真实支付 cost 并下注），检查运行时错误、层级分布、结局多样性、每年档期数、量级分布、类型分布、**时代专属事件占比**、填充事件占比、月份降级率 |
| **把柄 / 人脉 / 事件链 / 在位时长** | `lev` 可加/可花/不可为负、会顶活跃度（`bonusPressure.leverage`）；人脉 `hasContact` / 好感夹取 ±100 / `myContacts` 排序 / `forget` 断交 / 人名存在；**人脉引用完整性**（`contacts`/`req.contact`/`effects.contact`/`effects.forget` 的每个 id 都在 `reg.contact` 里）；**事件链闭合**（`after.id` 存在、不自环、不成环）与**窗口门控**（间隔不在 `min/maxMonthsAfter` 内不出现）；续集加权实测（≈ `chainWeightMul/(chainWeightMul+1)`）；`minTenure` 合法性、`prog_*` 是否都带 `minTenure`、`monthsAtTier` 门控与换层清零；存档迁移补齐 `lev`/`contacts`/`doneSeq`/`tierSince` |
| **静好岁月 / 大模型适配层** | 片段注册完整性（`slot` 合法、`text`/`texts` 非空、`id` 唯一、`weight` 为正、引用的 era/track/stance 存在）；**时令覆盖 12 个月**；**除时令外每个槽位都有无条件兜底片段**；跑遍 **时代×轨道×层级×月份（3×5×6×12）** 与 **9 种极端处境**（病重/穷困/巨富/丑闻/把柄/垂暮…）确认都拼得出文字；`settleQuietMonth` **幂等**（同一月只结算一次）与**跨年自动翻页**；成长**不越界**（`attrCap`/声望/好感 100）且**随年龄衰减**（25 岁长得比 70 岁快）；关开关后完全不成长；`renderQuiet` **只读不写**（重复渲染不改存档）；LLM **未配置即同步降级**、prompt **不含密钥**且**随年代收缩"还不存在的媒介"**、`cacheKey` 稳定、**缓存命中不重复花钱**、**缓存限容**、**额度上限生效** |
| **事件配图（照片层）** | 登记的类型 key 都在 `reg.category` 里；**逐个 `fs.existsSync` 检查照片文件真的在磁盘上**（最易静默翻车处）；有照片的类型出 `<img>` 且 `src` 指向登记文件、带 `.art-back` 垫片与 `onerror` 降级钩子；**没登记的类型必须退回程序化 SVG 且不出现空壳**；`ev.photo` 自定义文件名生效、`ev.photo=false` 关掉；同一事件两次渲染结果一致 |
| 引擎/内容契约 | 运行期动态注册新 era/event/effect 键，验证引擎自动接纳 |

### 9.2 `tools/smoke-ui.js`（jsdom，改 UI/机制时跑）

真的起一个 DOM，把「标题 → 建角 → 年卡 → **月历卡** → 档期事件 → 量级/类型/媒介徽章 → 日期 → 背景卡折叠展开 → 代价标签 → 缺资源变灰 → **把柄 / 人脉面板** → 投注面板 → 加减码 → **投注上限护栏（＋ 到顶置灰、投不起时说明原因）** → **死局保护（全堵死时放行保底选项）** → 确认判定 → D&D 明细 → 结算 → 继续 → **静好岁月卡（含成长小结）** → **AI 设置面板** → **年终结算（含年末补写的静好岁月）**」整套点击一遍。

其中两节会额外断言一些容易被改坏的事：
- **把柄 / 人脉**：① 人脉面板真的显示联系人姓名（不是 id）；② `cost:{lev:1}` 的选项在把柄不足时置灰并标「缺少把柄」；③ `req.contact` 未满足时标「需要先认识「某人」」；④ 事件链续集出现时，顶部有一条 **「承 前」小条**，写着上一幕标题与相隔几个月。
- **静好岁月 / 大模型**：① 平静的月份渲染出 `.vig` 卡、标题与月份范围正确、每月自成一段；② 成长小结把多个月**合起来算**（`CHA +2 · INT +1 · 声望 +1`），月历条上原有的「平静」标记**没有被挤掉**；③ 平静过久时只展开 `maxShown` 段、其余收成「另有 N 个月」；④ 没接大模型时给的是「接入大模型来润色」入口，点开弹出设置面板并讲清跨域限制；⑤ 保存后工具条 AI 按钮同步成「已接」；⑥ **请求未回/失败时正文仍是引擎拼好的那一段**（离线优先），并且有状态提示而不是静默卡住。

- **事件配图**：① 有照片的类型真的渲染出 `.art-photo` / `<img src="assets/events/xxx.jpg">`；② 左下角类型角标写着类型名；③ 底下垫着同类型的程序化 SVG；④ **真的派发一次 `error` 事件**，卡片切到破图态；⑤ 没登记照片的类型（灰产）渲染成 `svg.art` 而不是照片卡。

**改了 `engine/render.js`、`engine/time.js`、`engine/vignette.js`、`engine/llm.js`、`engine/dice.js`、`engine/art.js`、`engine/style.css` 或 `content/09-photo-art.js` 之后建议跑一次。**

```bash
cd ~/.workbuddy/binaries/node/workspace && npm install jsdom   # 只装一次
cd <game> && NODE_PATH=~/.workbuddy/binaries/node/workspace/node_modules node tools/smoke-ui.js
```

> 引擎本身零依赖；jsdom 只有 UI 冒烟用到，装在隔离工作区，不进游戏目录。

**接入 CI 建议**：pre-commit 或 CI 里跑 `node tools/validate.js`（退出码非 0 即拒绝）；涉及 UI 的 PR 再加跑 `smoke-ui.js`。

---

## 10. 已知限制与路线图

**当前限制（v0.4.0）**
- **内容量仍是最大的瓶颈，但已从"明显不够"变成"够玩一局"**：61 个手工事件（`major` 14 / `mid` 30 / `minor` 17）、16 个 `unique`。
  而"一个月一回合"的节奏下一年要消耗 4~9 个档期（模拟 300 局实测**每年平均 8.4 个档期、约 4 个月有事发生**），事件池深度仍直接决定体验质量：
  - 填充事件占比 0.0%（健康），
  - 日期自洽率 100.0%（`month` 事件一多就会掉，要盯），
  - 时代专属事件占比 21.8%（每时代 20.4%~23.1%），`balance.eraWeightMul=3` 已经顶过一次，再稀释就要继续加时代内容。
  **当前性价比最高的活仍然是加事件**，按 `时代 × 类型` 切片派给不同的人（见 §5.4.3）。
- **平静的月份现在也有内容了**（v0.4 的「静好岁月」）：实测**每年约 8 个月是平静的**，
  所以随笔素材库（`content/08-vignettes.js`）其实是**消耗量最大的一类内容** ——
  每局平均要结算 **408 段**随笔（对比每局 437 个事件）。素材只有 65 条，
  靠 `texts` 多写法 + 六槽位组合撑住变化，**下一次扩内容时它就是第一顺位**。
- 平静月的成长（属性/声望/人脉）会带来一点**权力爬升的加速**：实测打到 T5 从 151 升到约 160（+6%），因病去世从 126 降到约 110。
  这是"平静的年份也在长本事"的必然结果，量级可接受；但 `balance.vignette.hpChance` 千万别开大（见 §5.10 的实测表）。
- 大事件默认一局只演一次（`unique: true`），当前大事件池 **14 个**（比 v0.3 的 6 个翻了一倍多），但玩到中后期仍可能"大事件演完"。
  解法仍是**多写大事件**，或给某些确实可重复的大事件写 `unique: false`。
- **事件链已经能写**（`after` + `balance.chainWeightMul`，样板见 `84-archive.js` 的四幕链：拿到→被反噬→摊牌→了结），
  但**多阶段竞选还没有** —— 竞选仍是单个事件，没有"初选→大选→组阁"的链。
- **人脉系统已上线**（`reg.contact` + 好感 ±100 + 断交，共 10 个联系人：兄弟/掮客/高利贷/私人医生/工会头目/牧师/专栏作家/制片人/说客/联邦探员），
  但它本质是"有名字的 NPC + 好感度"；**NPC 关系图 / 关系面板还没有**（`reg.npc` 预留未实现）。
- **大模型适配层已上线**（`engine/llm.js`，见 §5.11），用来润色「静好岁月」或另写一段；
  但它**只服务这一处**：新闻标题仍是离线模板，传记/结局文还没有接入。
- 轨道切换（"变节"）只有设计，未实现。
- 资源经济已完整上线（`cost` / `stake` / 判定明细 / 把柄 `lev`），61 个事件都已有 `brief` / `grade` / `category`；
  但 **`cost`/`stake` 目前只有演示包 + 灰产/媒体/档案三条线在用** —— 把资源机制补进其余正经事件仍是另一件高性价比的活。

**路线图（按引擎/内容拆分）**

| 里程碑 | 引擎侧 | 内容侧 |
|---|---|---|
| M0（已完成，v0.3） | 月度回合（年→月→档期）、时代压力、大/中/小事量级、事件类型注册表 + 每类型默认配图、时代媒介时间轴、时代脚本定点事件、存档迁移 v0.2→v0.3 | 事件补齐 `grade`/`category`；新增 6 个跨媒介时间轴事件；三时代补 `pressure` + `scheduled` |
| M1（已完成，v0.4.0） | **事件链（`after` + `chainWeightMul`）**、**把柄资源（`lev`）**、**人脉系统（`reg.contact`）**、**晋升在位时长（`minTenure`）**、存档迁移 v0.3→v0.4 | 事件从 42 扩到 **61**（新增灰产 6、媒体 4、档案四幕链 4、族群飞地 5、三时代各 +5）；把柄/人脉写进内容；灰产 `category` |
| M1.5（已完成，v0.4.0） | **静好岁月**（平静月的随笔拼装 + 成长结算，`engine/vignette.js`）、**可选的大模型适配层**（离线优先 / 超时降级 / 缓存 / 额度上限，`engine/llm.js`） | 静好岁月素材库 65 条（六槽位 × 三时代 × 五轨道 × 各层级 × 若干心绪） |
| M2 | NPC 关系图（`npc` 注册表 + 关系面板）；多阶段竞选（把 `after` 用在竞选链上） | 同僚线 / 爱情线 / 家族线；竞选三幕链；**继续扩静好岁月素材**（消耗量最大） |
| M3 | 把 AI 适配层铺到别处：新闻标题、结局评语、传记式年终回顾（仍守"离线优先"） | 对应的 prompt 模板 |
| M4 | 轨道切换与"变节"结算 | 各轨道专属事件 |
| M5 | 平衡调优（模拟器扩到 1000 局） | 12 个时代锚点补齐 |

---

## 10.5 v0.5 新机制速览（出生州 / 掷骰建角 / 下野 / 收益面板 / 年终随笔）

**目录三层结构（先记住这个）**：项目根 = `dev/`（开发区，引擎+内容+工具）+ `dist/`（可分发游戏本体，`bash dev/tools/package.sh` 生成）+ `docs/`（文档在项目根，不在 dev/ 里）。改完代码固定两步：`node dev/tools/validate.js` → `bash dev/tools/package.sh`。

| 机制 | 内容包怎么写 | 引擎在哪 |
|---|---|---|
| 出生州 | `content/12-states.js`：`{name, lean: D/R/S, strength:1-3, entryEffects}`；事件门槛写 `states:[...]`；倾向表 `when.states` | core.js `reg.state`/`stateWindFor`、when.js `states` |
| 定命一掷 | `balance.rollAttrs{min,max,rerolls}` / `freePoints` / `freeCapPerAttr` / `vipCodes{码:点}`（码一用记 localStorage） | render.js `rollAttrs/rerollAttr/spendAttr/applyVipCode` |
| 下野（软 BE） | 事件效果键 `fall: 1或2`（降级+声望重挫+fallen标记+12月保护期）；下野后 tier≥3 退休 → `retire_comeback` 结局 | effects.js `fall`、render.js `afterEvent` 出一页交代卡 |
| 硬结局 | 效果键 `hardEnd: "prison"\|"disgrace"` → 终局直接收口 | effects.js `hardEnd`、40-endings.js `disgrace` |
| 路线转向 | 效果键 `setTrack:"operative"` / `setStance:"outsider"`（选项直接改轨道/姿态） | effects.js |
| 收益面板 | 无需配置——`resolveChoice` 自动把 outcomes.effects 翻译成筹码（tagNames 里登记过的 flag 才翻译） | render.js `gainSummary/gainBoxHTML` |
| 选项说明 | 选项可写 `note:"这条路意味着什么…"`（折叠在按钮里，点说明不触发选择） | render.js presentEvent |
| 年终随笔 | `content/13-year-tales.js`：`{kind: did/gain/now, when, texts[]}`，when 支持 upRep/downFun/busyYear/quietYear/upTier/fallen 等差值布尔 | render.js `yearNarrative` |
| 静好轨道分化 | `balance.vignette.trackBonus:{electoral:{rep:1.6}, wealth:{fun:0.002}, …}`（只加正向乘子；改完重跑 300 局） | vignette.js |

**事件卡新顺序**：标题 → 承前 → 正文 → 配图 → 背景卡（折叠）→ 选项（note 折叠）。结算顺序：结果叙事 → 收益筹码 → 判定明细（折叠）。

**多线程/并行开发的边界**（常被问）：引擎机制改动集中在 engine/，必须串行（render.js 被多个机制共用，并行必冲突）；**内容扩充天然可并行**——不同的人写不同的 `content/events/*.js`（按 时代×类型 切片），文件互不重叠，各自跑 validate.js 后合并即可。这正是"引擎+内容分离"的设计目的。

## 11. 排错 FAQ

**Q：双击 `index.html` 是空白页？**
A：打开浏览器控制台看报错。最常见是内容文件里语法错误 → 跑 `node tools/validate.js` 定位。

**Q：新加的事件不出现？**
A：① 文件的 `<script src>` 有没有登记在 `index.html`；② `era` 是否匹配当前开局时代；③ `tierMin/tierMax`、`tracks` 是否把你自己排除了；④ `weight` 是否为 0；⑤ **写了 `month` 吗？**写了就只在那个月才可能抽到；⑥ **`medium` 依赖的媒介在这个年代还不存在**（比如 1960 年抽不到短视频事件）。

**Q：事件出现的月份明显不对（9 月的事件出现在 3 月）？**
A：这是引擎的第三级降级——其余量级在本月都没有「就是本月」或「没写月份」的事件，只能放开月份限制。
根因是事件池太浅，不是 bug。看 `validate.js` 的「日期自洽率」，加内容即可改善。

**Q：玩着玩着全是"一桩地方丑闻"？**
A：事件池被抽空了。看 `validate.js` 的「填充事件占比」。
注意现在的降级链是：本月事件 → 无月份事件 → 放开月份 → **放开去重窗口** → 才轮到填充器，
所以填充占比高只说明**硬条件（era / tier / 媒介 / 年份）下真的一个事件都没有**，通常要加内容或放宽 `tierMin/tierMax` 区间。

**Q：大事件怎么玩到中期就再也不出现了？**
A：大事件默认 `unique: true`（一局只演一次），当前大事件池 14 个（v0.3 是 6 个），长局仍会被消耗光。
**多写大事件**是唯一解法；确实需要重复的话给那个事件写 `unique: false`。

**Q：写了事件链（`after`），但续集从来没出现过？**
A：按顺序排查五点：
① **前一幕没演过** —— `after.id` 写的是上一幕的 `id` 吗？链条必须真实发生过才会解锁；
② **间隔不在窗口内** —— `minMonthsAfter` / `maxMonthsAfter` 是"距上一幕几个月"，太早太晚都不会出（引擎按 `monthsSince(prev)` 判）；
③ **前一幕的 `flags` 没打上** —— 晚一幕如果声明了 `flags:["xxx"]`，而上一幕的**你期望的那条路**没打这个标记，续集就永远不 eligible。别把"前情"当必然（样板见 `84-archive.js` 的第一幕：三条路里只有两条打 `archive_taken`）；
④ **没写 `unique: true`** —— 不写的话同一幕会重复演，链条穿帮；
⑤ **权重被稀释** —— 续集解锁后权重会乘 `balance.chainWeightMul`（默认 9），实测占该池 ≈ 88.6%，正常不会被压掉；如果确实没出现，先查前四条。
`validate.js` 的「把柄/人脉/事件链/在位时长」一节会报 `after.id` 不存在、自环、成环的写法。

**Q：把柄（`lev`）怎么都攒不上去？/ 人脉面板是空的？**
A：两件事分开放：
- **把柄**只能从灰产线事件（`80-shady.js`）+ 少数时代专属事件里赚，且**它不是"存款"而是"消耗品"**——`bonusPressure.leverage` 只在 `lev ≥ 3` 时才顶活跃度，
  所以要么攒到 3 份以上，要么就当一次性的"武器"花掉（`cost:{lev:1}`）。另外每年有 `balance.leverageDecayChance`（34%）的概率掉 1 份——**放着不用会烂**。
- **人脉面板空**说明你还没"认识"任何人。人脉是通过事件里的 `effects.contact:{id:...}` 首次获得（第一次见面即"认识"，初始好感写在 `reg.contact[id].favor`），
  不是开局就有的。想快点点亮面板，去走 `80-shady.js` / `82-press.js` / `86-enclave.js` 里的接触型事件。

**Q：层级升不上去？**
A：三件事都查一下：
① 是否有 `prog_*` 事件覆盖当前 `tierMin/tierMax` 区间，且其成功档位带 `effects.tier`；
② `prog_*` 现在**都带 `minTenure`**（在当前层级坐满 N 个月才放行），刚升上来要等几个月，看 `P.monthsAtTier()`；
③ 该 `prog_*` 是否已经 `unique` 演过（一局只演一次）。

**Q：遇到一个事件，所有选项都点不动，卡住了？**
A：内容 bug —— 这个事件的所有选项都带 `cost` 或 `req`，而玩家资源见底。
① 引擎有保险丝：`P.fallbackIndex(chs)` 会强行放行一个（优先"只是资源不够"的那个），
按钮上标「⚠ 保底选项：…，硬撑一次」，所以**正常情况下不会真的卡死**；
② 正确的修法是给这个事件补一个既无 `cost` 也无 `req` 的**保底选项**（见 `CONTENT-SCHEMA.md` §4.3.1）；
③ `validate.js` 现在会直接报出违规事件，跑一次就知道是哪个。

**Q：投注面板里点了 ＋ 没反应 / 能投到 12 点精力，但加成早就封顶了？**
A：这两个是同一类 bug：档数上限过去只按"身上有多少资源"算，没按"加成上限"算。
现在统一走 `P.stakeMax(k, choice)`，被两道闸夹住：
① `ceil(cap ÷ w)`——超过部分没有收益（精力 +3%/点、上限 +9% → 最多 3 点）；
② 手上余额。所以：
- 一档都投不起 → ＋ 置灰并写「资金不足：每档需 $250k，你现在只有 $50k」，不会"点了没反应"；
- 投到顶 → ＋ 置灰并写「已达上限 +9%（最多 3 点）」；
- `P.stakeInfo()` 在扣款前会再过一遍这个闸，界面就算被改坏也不会多扣资源。
写自定义 `stake` 汇率时记得核对 `cap ÷ w ≥ 1`，校验器会拦 `cap < w` 的写法。

**Q：改了引擎，别人的内容报错？**
A：说明破坏契约了。回滚，或按 §6.3 用兼容方式改，并更新 `CONTENT-SCHEMA.md`。

**Q：能不用 `file://` 而用本地服务器吗？**
A：可以（`python3 -m http.server`），但**不必要**。设计目标就是免服务器双击可玩。

---

## 12. 术语表

| 术语 | 含义 |
|---|---|
| **事件（event）** | 一次叙事+选择节点 |
| **月（month）** | 一个回合。一个月最多若干"档期"，也可能整月平静 |
| **档期（slot）** | 某个月里的一个事件槽位；本月有几个、什么量级由时代压力决定 |
| **时代压力（pressure）** | 0-6 的数字，写在 `era.pressure`；决定某年有多忙、大事件多不多 |
| **活跃度（bonus）** | 丑闻/被调查/选举年/高层级叠加出的额外压力，让日程变密 |
| **量级（grade）** | 事件的分量：`major` 大事件 / `mid` 中事件 / `minor` 小事 |
| **类型（category）** | 事件的主题分类（仕途/舆论/丑闻/金钱…），每个类型自带默认配图与配色 |
| **媒介（medium）** | 事件依赖的"说话方式"（报纸/电视/互联网/短视频…），按年份自动放行 |
| **时代脚本（scheduled）** | 写在 `era.scheduled` 里的定点事件：到了某年某月必定发生 |
| **投注档（stake step）** | 投注面板里按一次 ＋ 的粒度：资金一档 `$250k`、精力一档 `1` 点。可投档数受 `ceil(cap÷w)` 与余额双重限制 |
| **保底选项（fallback）** | 一个事件里既无 `cost` 也无 `req` 的那个选项。保证玩家资源见底时仍能推进；`validate.js` 强制每个事件都有 |
| **档位（tier）** | 掷骰结果五档：crit / ok / meh / fail / critfail |
| **层级（Tier T0-T5）** | 权力高度；多轨道在此收敛（T0 无名 → T5 顶点） |
| **轨道（track）** | 晋升方式：选举 / 委任 / 名人 / 操盘 / 财富 |
| **起点（entry）** | 进入政坛的方式，决定开局资源与建议轨道 |
| **姿态（stance）** | 建制派 / 反建制 |
| **标记（flag）** | 状态位，驱动条件与结局（如 `scandal_2`、`investigation_open`） |
| **把柄（leverage / `lev`）** | 用脏手段换来的"胁迫本钱"。可加、可花（`cost:{lev:n}`）、不可为负；持有 ≥3 份时会顶活跃度（`bonusPressure.leverage`），每年有 `leverageDecayChance` 概率烂掉 1 份。**只当消耗品，不当存款** |
| **人脉（contact）** | 有名字、有好感度的常驻 NPC。首次通过 `effects.contact` 认识，好感区间 -100~100；`req.contact` 门控选项、`effects.forget` 断交。注册表在 `content/07-contacts.js` |
| **事件链（chain / `after`）** | 跨月/跨年接续的"一件事"。`after:{id, minMonthsAfter, maxMonthsAfter}` 声明前情；前一幕演过后续集权重 ×`chainWeightMul`（默认 9）。样板见 `84-archive.js` |
| **承前条** | 事件卡顶部的「承 前」小条，显示上一幕标题与相隔几个月，告诉玩家这是链条而非随机事件 |
| **在位时长（`minTenure`）** | 事件附加的门槛：在当前层级坐满 N 个月才可能出现（`P.monthsAtTier()`）。用来给晋升减速、避免同层级被反复刷 |
| **静好岁月（vignette）** | 没有档期的「平静月」里自动拼出来的一段叙事随笔（六槽位：时令/世相/案头/日常/心绪/收束），让主角在无事发生的时候也按部就班地长大。素材在 `content/08-vignettes.js`，逻辑在 `engine/vignette.js`。**只读渲染、不产生选项、不写数字** |
| **槽位（vignette slot）** | 静好随笔的六个拼装位置之一，按顺序取片段拼成一段文字；时令槽位吃月份，其余槽位吃时代/轨道/层级/心绪条件 |
| **平静月结算（`settleQuietMonth`）** | 平静月的成长与叙事落账点，挂在时间轴（`time.js`）上而非渲染层，保证 300 局模拟能覆盖、且重复渲染不会重复加成长 |
| **大模型适配层（LLM adapter）** | 可选的调味层（`engine/llm.js`）：模板文字永远先渲染，模型只在成功时替换；没配置/超时/报错都同步降级、绝不抛错、绝不阻塞。密钥只存 `localStorage`，prompt 里永不含密钥 |
| **照片层（photo layer）** | 事件配图的真图片层（`content/09-photo-art.js`），挂在 `POTUS.art(fn)` 这个最高优先级的挂载位上。能给照片就给，给不了 `return null` 让给程序化 SVG；照片加载失败时用预垫的 `.art-back` 顶上。表在 `reg.photo` |
| **配图三级降级** | `P.artSVG` 的解析顺序：内容级渲染器（照片层）→ `ev.art` → `category.art` → 引擎兜底。**保证任何时候都有图** |
| **填充（filler）** | 事件池耗尽时的模板兜底，保证事件永不枯竭 |
| **模糊胜算** | 给玩家看的档位提示（渺茫/不利/五五开/有利/稳操胜券） |
