# 2026-09-20 工作日志

## 新项目启动：美国政治生涯模拟游戏（代号 POTUS）

**需求来源**：用户希望做一个文字驱动的政治生涯模拟 / 视觉小说游戏。

### 已确认的需求要点（来自用户原话）

- 技术不限，JS/HTML 即可
- **必须有存档/读档**
- 视觉小说 + 修仙模拟器式养成（属性/随机事件/结局评分）的混合形态
- 可在关键流程接入 AI 接口能力
- 图片：可用网上公开图片，不做 AI 生图；无图也可纯文字
- **目标流程时长 > 1 小时，最好 10 小时**
- 支线必须丰富
- 背景参考真实世界历史事件；参考《纸牌屋》《华尔街之狼》《大空头》
- 主角：刚毕业的美国小伙，从政之路
- 玩法：图文叙事推进 + 选择节点多路线；选择后通过 **roll 点按概率进入不同结果**，再导向不同路线
- 开局时间可选（1910-2022 范围内），倾向做成「几个大时代锚点」让玩家选（类比三国选黄巾之乱/赤壁之战）
- **用户要求：动手前先输出文字版 PRD 供确认**

### 本次产出

- `/Users/dfiuser/WorkBuddy/2026-09-20-09-26-57/PRD_美国政治生涯模拟器.md`（v0.1，待评审）

### PRD 核心设计决策（待用户确认）

1. 时代锚点 12 个，首批建议 3 个：`1960_CAMELOT` / `1974_WATERGATE` / `2008_CRASH`
2. 四维核心属性 CHA/INT/CUN/INTG（诚信是路线约束，非越高越好）+ 6 种资源 + 10+ 派系好感
3. 生涯阶梯 11 级（志愿者 → 总统 → 历史评价），1 回合 = 1 年，55-65 回合
4. 掷骰模型：`P = clamp(Base + Σ修正, 0.05, 0.95)`，五档结果（大成功/成功/勉强/失败/大失败），对玩家只显示模糊胜算提示；可用人情点重投、资金加码
5. 14 条并行支线（家族/爱情/金钱/腐败/媒体/司法/同僚/理念/健康/家庭/金融/谍报/暗杀/帝国）
6. AI 接入 7 个点：开局综述、动态支线、NPC 对话、新闻头条生成、智囊建议、终局传记、自由行动模式；**异步不阻塞 + 6 秒超时降级 + 缓存 + 次数上限**
7. 内容三层架构：L1 手工核心 8-10 万字 / L2 模板化组合撑 60% 事件量 / L3 AI 动态。原因：纯手工写 25-35 万字不现实
8. 技术栈推荐：原生 HTML/CSS/JS，零构建，单文件可分发；种子化 RNG + 版本迁移链存档
9. 美术推荐：程序化 SVG/CSS（报纸头版、档案卡片、时代配色皮肤），历史照片作为可选开关
10. 里程碑 M0 引擎原型 → M1 单时代竖切（2008）→ M2 扩至 3 时代 → M3 AI 层 → M4 补 12 时代 → M5 打磨

### 合规约束（自行设定，待用户确认）

- 真实历史人物只作背景，绝不虚构在世真人的负面/犯罪/私德内容
- 对手/盟友/记者/金主等全部虚构
- 成人题材克制处理；全篇不涉及中国政治

### 待用户拍板的 9 项

① 做几个时代 ② 真实人物尺度 ③ 技术栈 ④ AI 接入时机 ⑤ 美术方案 ⑥ 语言 ⑦ 单局时长目标 ⑧ 尺度 ⑨ 游戏名

### 用户偏好观察（本项目）

- 习惯「先评审文档再动手」，明确要求先出 PRD
- 关注内容规模是否可实现（已在 PRD 中如实说明 25 万字手工不现实，给出三层架构方案）
- 要求列出待确认项而非替用户决定

### 下一步

等用户确认 9 项决策 → 修订 PRD 至 v1.0 → 开做 M0 引擎原型（可点可玩的骨架 + 存档读档）

---

## 追加：PRD 升 v0.2 + M0 引擎原型已交付

### 用户第二轮反馈（已落入 PRD v0.2）

1. **丰富可能性**：反对单线升职路线。加入 民主党/共和党/独立、社区工作起步/直接应聘政客团队/先做律师等公众人物/先做明星积累声望/NGO 路线、以及川普式"邪路"一步登天。
2. **合规放宽**（用户明确决定）：允许真实历史人物出现（含已被公开的丑闻/犯罪）、允许已故历史人物进入游戏、允许现实州/选区/公司名称；暴力/腐败/性/成瘾题材不受限制；**仅内部测试、非公开发行**。
   - 主代理保留的唯一硬约束：仍不涉及中国政治（平台合规）。
3. **时间模型**：不一定一回合=一年，一年可以有很多大事件；要增加随机性。

### PRD v0.2 关键改动

- 第 3 节合规边界按用户决策放宽重写
- 第 4 节改为 **beat（事件驱动）模型**：每年事件数 = 基础 + 活跃度 + 随机（1-10），新增随机中断事件与**黑天鹅表**
- 第 6.4 节新增 **起点路径** 6 种；第 8 节从"11 级阶梯"改为 **可能性网**：起点路径 × 晋升轨道(5) × 党派姿态(3党×2姿态) ≈ 90 种开局，权力层级 Tier T0-T5 多轨道收敛，终局不再只有总统
- 第 10 节支线扩到 16 条（新增党派/立场线、命运/随机线）

### M0 游戏原型（已交付，全部校验通过）

产物：`game/index.html`（单文件，零依赖，双击即玩）+ `game/README.md`

已实现：3 时代 / 6 起点 / 5 轨道 / 3 党 × 2 姿态 / 23 个手工事件（含 9 个跨层级晋升事件）/ 模板填充器（事件永不枯竭）/ 五档掷骰 / 黑天鹅 / 报纸风程序化美术 / 存档（自动+手动+文件导出导入）/ 结局评分。

---

## 追加批次（v0.5）· 文学性 + 历史浪潮 + 掷骰建角 + 州 + BE + 目录分层

### 用户需求（五批合并）

1. 月度/年度/结算过渡要有**文学性**（基于做了什么/得到/失去/处境，不要"声望4,资金400k"）
2. **时代历史事件太少**：战争/经济危机/水门；同一事件按主角层级分化版本；无政府背景可投入浪潮换政治资本
3. 事件要有**收益结算面板**（曝光/民意/人情/派系，可正可负）
4. **roll 点建角**：初始四属性掷骰 + 自由点分配；VIP 充值码加点（VIP1+1/VIP5+5/VIP50+50，测试码表）
5. 事件卡布局：描述→图→选项（选项下折叠说明）；反馈分清主次
6. **中途 BE**：入狱=硬结局；下野=软 BE 可东山再起
7. **输出分门别类**：文档/开发/可分发三个文件夹
8. **出生州选择**：各州政治倾向；顺风/逆风/摇摆；反其道拉拢少数派
9. **路线抉择事件**：参选vs幕僚、NGOvs体制、州参议院vs联邦众议院、竞选团队vs州长
10. 顺手完成：静好岁月按轨道分化自然增长；多线程问题答复（引擎串行/内容可并行）

### 引擎侧改动（v0.5）

- **core.js**：reg 新增 `state`/`yeartale` 桶；`rollAttrs/freePoints/freeCapPerAttr/vipCodes` 进 BALANCE_DEFAULTS；`stateDef/stateName/stateWindFor`；`vipActivate/vipUsedList`（一码一用记 localStorage）；migrate 补 `state/fallenCount/fallenShieldUntil/yearStartSnap`
- **when.js**：`states/notStates` 白黑名单 + snap 加 `state`
- **effects.js**：新效果键 `fall`（软BE：降级+声望重挫+调查了结+丑闻降2档+12月保护期）、`hardEnd`（硬BE：pendingHardEnd → afterEvent 收口）、`setTrack/setStance`
- **render.js**：
  - 建角：出生州组 + 定命一掷（roll/reroll/spend/VIP码）+ confirmCreate 吃掷骰结果 + 州顺风逆风起点派系
  - `gainSummary/gainBoxHTML`：effects→玩家语言筹码（tagNames 登记过才翻译，scandal_n 内部标记不翻）
  - 事件卡新顺序：标题→承前→**正文→配图→背景卡（折叠）→选项（note 折叠）**
  - 结算新顺序：**结果叙事→收益筹码→判定明细（details 默认收起）**
  - `yearNarrative()`：did/gain/now 三段式年终随笔（差值来自 yearStartSnap 快照），排在数字条上方
  - afterEvent：hardEnd 收口；fall 出独立「下野」交代页（continue 后再 nextSlot——直接 append 会被 nextSlot 冲掉，踩过）
- **vignette.js**：`trackBonus` 按轨道分化（electoral rep×1.6 / wealth fun+0.002 / operative contact×1.5 / appointment attr×1.3 / celebrity rep×1.3），只加正向
- **style.css**：rollgrid/rattr/gainbox/gchip2/ytales/chnote/details.check

### 内容侧

- `12-states.js`：10 州（摇摆 OH/FL/PA、深红 TX/AL、深蓝 NY/MA、略偏 GA/MI）
- `13-year-tales.js`：14 条年终随笔素材（did/gain/now 三 kind 各有兜底）
- `54-era-waves.js`（8 事件）：越战征兵 T0 版/T2+表决版、三月向华盛顿、石油危机加油队、水门泄密停车场（critfail+调查→hardEnd prison）、占领华尔街、茶党市政厅、底特律听证（no 路线 critfail→fall 1）、校车之争——**同一历史按层级分化**+wave_* 浪潮标记
- `62-crossroads.js`（4 事件）：首个分岔（setTrack electoral/operative）、NGOvs党机器、州vs联邦、竞选团队vs州长（critfail→fall）
- `64-states.js`（3 事件）：深红州民主党人（结盟少数派/切割/熬）、摇摆州最后一周（空中战/地面战/挖坟/保票）、深蓝州共和党人（温和派/纯度/无党派职位）
- `01-config.js`：identityBias 加州地形规则（摇摆→political/media、深州→civil/media）+ cross_*/wave_* 余波倾斜 + trackBonus + tagNames 新词条
- `40-endings.js`：`disgrace`（F）、`retire_comeback`（fallen+tier≥3→A 东山再起）、`retire_fallen`（C）
- index.html 清单 +5 文件

### 目录重组

```
2026-09-20-09-26-57/
├── dev/     开发区（原 game/：engine+content+tools+index.html）
├── dist/    可分发（bash dev/tools/package.sh 生成，53 文件，双击即玩）
├── docs/    文档（DEVELOPMENT-GUIDE + CONTENT-SCHEMA，从 game/docs 上移）
└── PRD_….md
```
删除了 `index.singlefile.bak.html`。validate.js 的 ROOT 相对路径不受影响（__dirname 在 dev/tools 下）。

### 校验（全绿）

- validate.js 新增整节「v0.5」：州注册/引用/顺风逆风方向、掷骰范围/自由点两道闸（**spendAttr 是步进 API，测试要循环+1 而不是传 99**——引擎没错，测试写错了）、VIP 一码一用、fall 降级+保护期+丑闻降档、hardEnd 收口、retire_comeback 优先级、setTrack 拒绝非法值、gainSummary 翻译与 scandal_n 屏蔽、年终随笔全处境可拼、note 类型
- 300 局模拟：结局分布 `retire_high 133 / death 100 / retire_low 30 / kingmaker 33`，T5 214，filler 0%，类型分布明显更丰富（civil/political/shady/media 四强）
- smoke-ui.js：新增「v0.5 掷骰建角/VIP/州/收益面板/下野」整节（60+ 断言），修复 3 处旧断言（建角要选州+掷骰才能进；VIP 输入框在掷骰块里；灰产现在有照片了→改用动态无照片类型）。**跑 3 次不 flaky**
- 无头 Chrome 截图 dist/index.html：报纸风格渲染完美，77 事件 13 结局规则加载

### 踩坑（新增）

1. **DOM stub 的 validate.js 没有 querySelectorAll**——renderCreate 用了它，一跑就崩。stub 加 `querySelectorAll: () => []` 即可（真 DOM 覆盖交给 smoke-ui）
2. **fall 交代卡不能 append 完就 nextSlot**——nextSlot 会重写 #main 把卡片冲掉。必须渲染成独立页（继续按钮→nextSlot）
3. **smoke 测试里选择器引号转义**：Python 注入 JS 时 `'...["\'' + k + '\'"]'` 这种嵌套引号极易写坏——统一改成 `".opt[onclick*=\"" + k + "\"]"` 双引号外层
4. 旧的「灰产没照片」断言过期了（照片层后来补了 shady.jpg）——**涉及内容量的断言要写成动态的**（找当前无照片的类型），不能钉死某个 key
5. 300 局结局分布变化在预期内（T5 214 vs 之前 168）：新事件的 tier:+1 选项多了，属内容变多的自然结果，非平衡崩坏

### 复用要点

1. **软 BE（下野）的关键设计**：fall 效果键 + fallenShieldUntil 保护期（12 个月防连环犁穿）+ 事后结局规则认 fallen 标记（retire_comeback）——"BE 但游戏继续"需要三件套，缺一不可
2. **收益面板翻译层**（gainSummary）要把内部标记（scandal_n/bs_*）屏蔽掉，只翻 tagNames 登记过的——否则玩家看到一堆调试字符串
3. **主次分明的结算顺序**：叙事结果 → 筹码式收益 → 折叠判定明细。判定明细改 details 默认收起后，玩家读起来不再是"先看 d100 再读故事"
4. **目录分层的最小动作**：只 mv game→dev、docs 上移、package.sh rsync 排除 tools/docs——validate 的相对路径零改动
5. **同一历史事件按层级分化**是时代浪潮的核心写法：T0 收到的是征兵令，T2+ 面对的是拨款表决——写成两条独立事件各带 tierMin/tierMax，比一条事件里写 if 分支干净得多

### 下一步建议

- 事件池 77 个仍偏薄（长局中后期重复）；下一批按 时代×类型 切片继续加
- 用户的 AI 推演示例（2010 开局 → 茶党 → SuperPAC → 2016 押注 → 参议员 → 总统）是很好的**名人轨道剧本参考**——可写成 celebrity/wealth 轨道的时代剧本事件链（2016 时代锚点已在 PRD 规划里）
- 用户提到「按现实历史每一年扩展事件」：era.scheduled + fromYear/toYear 窗口就是为此设计的，按年补即可


---

## 追加批次 v0.5.2 · 试玩反馈驱动的十连改（用户实测后连续 12 条反馈）

### 用户反馈 → 落地清单（全部完成、双校验全绿、dist 已刷新）

1. **背景卡移到选项下方**独立成框（brief-slot 容器）
2. **VIP 兑换码无限用**（vipInfinite=true 开关，正式运营翻回 false 即一码一用）
3. **状态栏职位卡**：职务（14-offices.js 真实职位名表）+ 选区规模 + 选民三池 + 政治光谱
4. **静好岁月彻底重做**（用户三条：散文太虚/不发工资/不许合并月）：
   - 平静月**逐月出卡**（advanceMonth 返回 "quiet" 也停留，手动点继续）
   - 月卡=这个月做了什么（quietWorks 素材：见选民/开会/巡排水，按轨道×层级筛）+ 这个月的账（工资-开销当场入账）+ 成长
   - **工资按职位实算**（officeSalary 表：市议员 $2.5k/月→联邦议员 $8k→总统 $33k；名人/财富线是版税分红更高）
5. **静好月随机变动**：资金小额进出(funChance 30% 双向)+精力回 1 点+人情偶得
6. **年龄曲线**：65+ CHA/INT 每月 6% 掉 1 点(下限30)、CUN 中年仍长(权重2)、**INTG 移出自然成长池**（诚信是选择）
7. **诚信→公信力**：UI 挪到声望旁带 tooltip；不设选项门槛只做判定加分；60 个 INTG 判定保留
8. **统一叫法**：CHA→魅力、人情往来→人脉好感（状态栏/成长提示/gainSummary 全对齐）
9. **状态 tooltip**：tagNames 升级 {name,desc,effect} 三元（60+词条全写），没登记的 flag 不显示原文
10. **事件经济审计**：funMul 比例效果键（投资型押多少赚百分比）；shady_oneshot 重写；validate 硬断言"≥$50k 投入的 ok 档必须有回报或资产"；金主晚宴逻辑修正（你是募款的不是花钱的）；2 处低回报修内容
11. **去 T 化**：所有"T2"显示→具体职位名（门槛文案/年终卡/状态栏/结局）
12. **选民池**（用户核心设计）：faction.base→"基层组织"；G.voters{warm,diehard,oppose} 三档具体人数；选区规模表(T0 5千→T5 2.4亿)；**升位只带 25% 旧地盘人心**；electionStrength 胜算读数；effects.voters 效果键
13. **晋升进度条**：在位40%+声望25%+选民20%+组织15%；60 分进"机会区间"（红刻度线），但晋升仍等 prog_* 位置空缺事件——进度决定你抓不抓得住，不直接给位置
14. **工会事件补前因**（罢工投票守登记台→秘书来电话）+ **after 余波链** shady_union_collect（两年后背书来兑账，兑现/讨价还价/赖账三路，赖账 forget 掉头目）
15. **设计原则文档化**（docs/CONTENT-SCHEMA.md §11.6）：经济诚实/门槛匹配身份/前因后果+深远影响/身份驱动/显示可懂/数字语义可追问——六原则，后续写事件必读

### 引擎新 API（v0.5.2）
- advanceMonth() 返回 "event"|"quiet"|false（逐月停留模型；旧调用方判 truthy 兼容）
- P.quietWorkLine/quietAccount/renderQuietMonthCard
- P.tagInfo/tagTooltip；P.tierName/officeName（reg.office/officeFallback/officeSalary）
- P.voterPools/applyVoters/rescaleVoters/electionStrength/electorateSize
- P.promotionProgress（{pct,ready,note,nextName}）
- effects 新键：funMul（比例资金）、voters（选民池）；tier 效果自动 rescaleVoters
- balance 新键：quietWorks[]、quietAccount{}、voterBase{electorate[],carryKeep}、attrDeclineAge/Chance/Floor
- vipInfinite 开关

### 踩坑（新）
1. render.js 大段中文 replace 用 python 逐行 splice 比 Edit 工具可靠（这个项目 Edit 对中文内容仍偶发"报成功没写入"）
2. advanceMonth 返回值从 boolean 变三态后，**所有循环调用方都要审**（validate 的 300 局模拟和年度循环都改了；smoke 的"进入1月必出选项"断言也要放宽成两路合法）
3. vignetteGrowth 的 notes 混入非属性条目后，attrAgeFade 断言要**只数属性条目**（按中文属性名过滤）
4. 消费型 vs 投资型支出的区分标准："ok 档有没有看得见的非钱回报（rep≥5/派系≥5/contact/flag）"——这个口径写进了 validate 和文档
5. jsdom 里 renderCreate 重建 DOM 后**旧元素引用失效**——VIP 二次兑换测试要重新 getElementById

### 用户已确认的方向（未完成的 backlog，按优先级）
- **#11 时代锚点扩展**：1972 水门全程/1990 海湾911/2016 当代——用户明确点名水门、海湾战争、伊拉克战争、911、新冠
- **#16+#23 历史事件逐年扩充**：用户要"近 100 年百大事件"进游戏；同一事件按 MC 状态/地位/资源多版本+多波及（系统性/长期深远）+ 互斥(unique/flags/notFlags)+上千事件互相影响
- **#17 成长路径**：检察官/法官线、名人轨道 2016 剧本（用户 AI 推演为蓝本）
- **#18 NGO 方向细分**（环保/人权/工会/社区）+ 每种职业的专属日常事件
- **#22 体育大赛政治事件**：1980/1984 奥运抵制、申办丑闻
- **#27 全面内容 review**（描述/经济/门槛三维逐文件审计）
- **#34 职业拐点常态化**：用户最新反馈"路线切换节点应更频繁如真实世界"——cross_* 部分转可重复+新增中程拐点
- 大模型接入暂停（用户：比较鸡肋，先完善游戏）
