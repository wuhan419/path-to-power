# 系统噪声
.DS_Store

# 本地日志 / 临时脚本输出
*.log
*.log.*
logs/
dev/tools/out/

# AI 助手生成的记忆 / 工作区元数据（一律不入库）
.workbuddy/
.qoder/
.cursor/
.aider*
.continue/

# 依赖（本项目零依赖，正常不会出现；防御性忽略）
node_modules/

# 美术出图临时产物（.miora 预览工程 + 同名 _assets 散图 + staging），不入库
/potus-*.miora
/potus-assets/
/potus-*_assets/

# itch.io 上传包（package.sh 产物，不入库）
/path-to-power-*.zip
